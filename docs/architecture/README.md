# Architecture Documentation

This folder holds the active technical source of truth.

Use it for system structure, component responsibilities, API shape, data flow,
database boundaries, security boundaries, deployment shape, and architecture
tradeoffs.

Start with `overview.md`.

# Onboarding Documentation

This folder holds baseline audits for existing projects.

When Project Harness is installed into a repo that already has code, create an
onboarding audit before using generated docs as source of truth. The audit
should distinguish confirmed, partial, unknown, and not implemented behavior.

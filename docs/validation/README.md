# Validation Documentation

This folder holds proof expectations and validation evidence.

- `test-matrix.md`: behavior-to-proof index.
- `reports/`: validation reports for notable changes.

Use `scripts/bin/harness-cli query matrix` when the Harness CLI is installed.
If the CLI is unavailable, read `test-matrix.md` directly and state that the
durable matrix could not be queried.
